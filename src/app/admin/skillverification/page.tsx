import VerificationRequests from  '@/components/Admin/skillverifications';

export default function SkillVerificationPage() {
    const userId = 'user123';
  return <VerificationRequests userId={userId}  />;
}

